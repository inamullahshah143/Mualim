import 'package:flutter/material.dart';

class StudyingCourses extends StatelessWidget {
  const StudyingCourses({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
