import 'package:flutter/material.dart';

class StuydingCourses extends StatelessWidget {
  const StuydingCourses({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
