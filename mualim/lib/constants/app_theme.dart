import 'package:flutter/material.dart';

class AppTheme {
  static const appThemeColor = Color(0xFF915DB1);
  static const primary = Color(0xFF431A60);
  static const fonts = Color(0xFF2B2B2B);
  static const secondary = Color(0xFF5E5E5E);
  static const placeholder = Color.fromARGB(255, 230, 230, 230);
  static const placeholderBg = Color(0xFFF2F2F2);
  static const white = Color.fromARGB(255, 250, 250, 250);
  static const red = Colors.red;
  static const transparent = Color.fromARGB(0, 255, 255, 255);
}

const double kDefaultPadding = 20.0;